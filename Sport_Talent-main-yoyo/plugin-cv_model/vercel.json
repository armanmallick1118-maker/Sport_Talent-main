{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && cd ../apps/web && npm install",
  "buildCommand": "cd ../apps/web && npm run build && cp -r .next ../plugin-cv_model/.next",
  "outputDirectory": ".next"
}
