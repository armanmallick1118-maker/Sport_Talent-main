{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && cd ../apps/web && npm install",
  "buildCommand": "cd ../apps/web && npm run build && rm -rf ../../plugin-cv_model/.next && cp -r .next ../../plugin-cv_model/.next && (cp -r public ../../plugin-cv_model/ 2>/dev/null || true)",
  "outputDirectory": ".next"
}
